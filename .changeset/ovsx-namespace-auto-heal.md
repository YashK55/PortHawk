---
"porthawk-core": patch
"porthawk": patch
"porthawk-vscode": patch
---

Fix Open VSX publishing: the release workflow now creates the Open VSX namespace automatically if it doesn't exist yet, instead of failing the release.
